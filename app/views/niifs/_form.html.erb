<%= form_with(model: niif) do |form| %>
  <% if niif.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(niif.errors.count, "error") %> prohibited this niif from being saved:</h2>

      <ul>
        <% niif.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_area :text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
