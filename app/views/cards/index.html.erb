<p id="notice"><%= notice %></p>

<div class="form">
  <%= render 'form', card: @card %>
</div>

<div class="container-fluid"> 
<table>
  <thead>
    <tr>
      <th>Mercado</th>
      <th>Nombre de la Banca</th>
      <th>Cuenta</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody id="cards">
    <% @cards.each do |card| %>
      <tr>
        <td><%= card.name %></td>
        <td>
          <% card.kinds.each do |ki| %>
          <%= ki.name %>
          <% end %>
        </td>
        <td>
          <% card.cates.each do |cate| %>
          <%= cate.name %>
          <% end %>
        </td>
     </div>
   
      </tr>
    <% end %>
  </tbody>
</table>


<div class="text-center">
<%= link_to 'Back', root_path, class: "btn btn-outline-danger" %>
</div>

