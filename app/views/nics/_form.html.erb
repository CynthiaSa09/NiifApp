<%= form_with(model: nic) do |form| %>
  <% if nic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nic.errors.count, "error") %> prohibited this nic from being saved:</h2>

      <ul>
        <% nic.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :number %>
    <%= form.number_field :number %>
  </div>

  <div class="field">
    <%= form.label :category_id %>
    <%= form.text_field :category_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
