<p id="notice"><%= notice %></p>

<h1>Categorias</h1>

<table class="container">
  <thead>
    <tr>
      <th>Cuenta</th>
      <th>Es publica</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cates.each do |category| %>
    <tr>
    <td><%= category.name %></td>
    <td><%= category.is_public %></td>
    </tr>
  <% end %>
  <br>
  </tbody>
  </table>