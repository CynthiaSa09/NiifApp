<%= form_with(model: cate) do |form| %>
  <% if cate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cate.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
        <% cate.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

   <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :is_public %>
    <%= form.check_box :is_public %>
  </div>

  <div class="field">
    <%= form.label :cate_id %>
    <%= form.text_field :cate_id %>
  </div>

  <div>
    <%= form.submit 'Submit', class:'bg-danger' %>
<% end %>
</div>